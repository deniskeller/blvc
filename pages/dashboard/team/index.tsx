import { Dashboard } from '@layouts/index';
import { Team } from '@view/dashboard';

const TeamPage = () => {
  return (
    <Dashboard>
      <Team />
    </Dashboard>
  );
};

export default TeamPage;
